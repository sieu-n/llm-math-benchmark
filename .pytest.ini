[pytest]
markers =
    expensive: marks tests as expensive (deselect with '-m "not openai"'). Especially when test actually calls openai instead of mocked version. Default is to skip these tests.

addopts = -m "not expensive" --html=pytest-report.html --self-contained-html 
